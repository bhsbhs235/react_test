language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

services:
  - mysql

before_install:
  #- mysql -u root --password="admin123^^" > test/travis.sql
  - mysql -e 'CRATE DATABASE user'
  - chmod +x gradlew

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - bhsbhs235@naver.com